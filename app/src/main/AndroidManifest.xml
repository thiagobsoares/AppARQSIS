<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.projeto.com.br.appservicedesk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuDeChamados">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NovoChamado" />
        <activity android:name=".ListarChamados" />
        <activity android:name=".ChamadoMensagem" />
        <activity android:name=".FecharChamados" />
        <activity android:name=".FecharChamadosLIsta" />
        <activity android:name=".ListaDeChamados"/>
    </application>

</manifest>